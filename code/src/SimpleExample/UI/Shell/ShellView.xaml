<Window x:Class="SimpleExample.UI.Shell.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ShellView" Height="600" Width="800">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="Child ViewModel status" VerticalAlignment="Center" Margin="12" />

        <Border Grid.Column="1" Background="Red" Width="300" Height="200">
            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="24" Text="Not Working" />
        </Border>

        <ContentControl x:Name="Child" Grid.Column="1" />

    </Grid>
</Window>